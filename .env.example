# Server Configuration
PORT=3000
NODE_ENV=development

# Database/Storage
USER_DB_PATH=data/users.json
# USER_DB_PATH=/tmp/tripmaker-users.json  # Use this for Vercel or read-only filesystems

# CORS Configuration
# Use comma-separated list of origins, or * for all origins
CORS_ORIGINS=http://localhost:5173,https://trip-maker-web.vercel.app
# CORS_ORIGINS=*  # Allow all origins (not recommended for production)

# JWT Authentication
# JWT_SECRET is OPTIONAL for development (auto-generated if not provided)
# JWT_SECRET=your-secret-here-optional-for-development
JWT_EXPIRES_IN=7d

# For Development: JWT_SECRET is auto-generated if not set
# For Production: JWT_SECRET is REQUIRED (server will exit if not set)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
